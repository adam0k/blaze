/*import {cleanup, render} from '@testing-library/svelte';
import Rooms from '/home/adam/1.TUKE/blaze/public/views/Rooms.svelte';

describe('Rooms', () => {
	
	afterEach(cleanup);
	test('render rooms', () => {
		const {getByText} = render(Rooms);
	});
});*/